# Copy this to .env and source it: `set -a; source .env; set +a`
# .env is gitignored — never commit real values.

PROJECT_ID="mars-114514"
ZONE="asia-east1-b"            # Changhua, Taiwan — lowest latency from TW
INSTANCE_NAME="apt-gpt-goad"
MACHINE_TYPE="n2-standard-16"   # 16 vCPU / 64 GB RAM, ~$0.97/hr on-demand
DISK_SIZE_GB="250"
NETWORK_TAG="apt-gpt-goad"
